#include <stdlib.h>
#include "erreurs.h"

char *err_malloc = "erreur d'allocation mémoire";
char *err_calloc = "erreur d'allocation mémoire (calloc)";
char *err_realloc = "erreur d'extension mémoire";
